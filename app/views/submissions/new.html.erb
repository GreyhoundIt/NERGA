<h1>Players information for <%= @venue %></h1>
<%= form_for(@submission,url: submissions_path(fixture: params[:fixture], league: params[:league]), :html => {:class => 'form-inline'})  do |f| %>
<%= render 'form', f: f %>
<% end %>
